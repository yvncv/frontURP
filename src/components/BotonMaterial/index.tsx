import { Catalog } from "../../types/Catalog";


const ModalMaterial = ({ catalog }: { catalog: Catalog }) => {
    return (
        <button className="btnMaterial">Ver Material</button>
    );
};

export default ModalMaterial;